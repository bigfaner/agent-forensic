---
status: "completed"
started: "2026-05-12 16:22"
completed: "2026-05-12 16:36"
time_spent: "~14m"
---

# Task Record: 2.1 Build SubAgent Inline Expand Component (UF-1)

## Summary
Built SubAgent inline expand component for the Call Tree panel. Extended visibleNode with depth/subIdx fields for SubAgent children at depth 2. Added subAgentExpanded and subAgentErrors maps to CallTreeModel. Implemented collapsed, expanded, error, and overflow states with emoji/ASCII fallback. Created errorLabel dispatch function, rendering helpers for SubAgent nodes and children, and overflow message. All 21 new tests pass with 82.1% model coverage.

## Changes

### Files Created
无

### Files Modified
- internal/model/calltree.go
- internal/model/calltree_test.go

### Key Decisions
- SubAgent expand state tracked via map[string]bool keyed by 'turnIdx-entryIdx' to avoid adding a separate field per node
- SubAgent errors tracked via map[string]error with same composite key — errors prevent expansion rendering
- Overflow (+N more) rendered inline in renderTree() rather than as a visibleNode to keep node count predictable
- errorLabel dispatch function maps parser error types to short display labels per tech-design spec
- ASCII mode controlled via SetASCIIMode() method for terminal detection integration in task 3.1
- renderSubAgentNode/Child/Overflow separated into distinct methods for clean state management
- maxSubAgentChildren=50 as constant matching design spec overflow threshold

## Test Results
- **Tests Executed**: Yes
- **Passed**: 388
- **Failed**: 0
- **Coverage**: 82.1%

## Acceptance Criteria
- [x] SubAgent children rendered at depth 2 with ├─ └─ tree connectors
- [x] Collapsed state: ├─ SubAgent ×3 (4.8s) 📦 (ASCII: [A])
- [x] Loading state: 📦 ⏳ suffix (ASCII: [A] ...)
- [x] Expanded state: children visible, navigable via j/k
- [x] Error state: 📦 ⚠ suffix (ASCII: [A] !), children hidden
- [x] Overflow: >50 children → │  ... +N more in text-secondary
- [x] Children sorted by JSONL appearance order
- [x] Error label dispatch function: errorLabel(err) maps error types to short labels
- [x] Unit tests: each state renders expected substrings; mock SubAgentStats data

## Notes
Loading state is handled via subAgentExpanded=false + subAgentErrors[nil] — the collapsed state naturally shows before expansion. The actual async loading trigger will be wired in task 3.1 (integration). The toggleExpand() method is NOT modified per task spec (deferred to 3.1).
