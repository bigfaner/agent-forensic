---
status: "completed"
started: "2026-05-10 07:52"
completed: "2026-05-10 07:56"
time_spent: "~4m"
---

# Task Record: 2.6 File Watcher

## Summary
Implement file watcher using fsnotify that monitors a directory for JSONL file changes, detects appends, and emits WatchEvent with FilePath, Offset, and Lines. Includes offset tracking, JSONL-only filtering, clean shutdown, and idempotent Start/Stop.

## Changes

### Files Created
- internal/watcher/watcher.go
- internal/watcher/watcher_test.go

### Files Modified
- go.mod
- go.sum

### Key Decisions
- 2.6: Used fsnotify for OS-native file change events, no polling fallback needed as fsnotify handles cross-platform
- 2.6: Offset tracking via map[string]int64 to remember last-known file sizes per path
- 2.6: Only .jsonl files are processed; all other file events are filtered out in handleEvent
- 2.6: Buffered channel (cap 16) for events to reduce contention between producer and consumer
- 2.6: New files discovered via Create event emit all existing lines; existing files only emit new appends

## Test Results
- **Tests Executed**: Yes
- **Passed**: 12
- **Failed**: 0
- **Coverage**: 84.6%

## Acceptance Criteria
- [x] internal/watcher/watcher.go implements the Watcher interface with Start(), Stop(), Events() <-chan WatchEvent
- [x] WatchEvent carries FilePath, Offset, and raw new Lines
- [x] Detects file appends within 2 seconds
- [x] Stop() cleanly closes the watcher and channel
- [x] Integration tests using t.TempDir() for isolated fixture files
- [x] 3-second timeout on test waits to prevent hangs
- [x] Test coverage >= 80%

## Notes
fsnotify provides cross-platform support including Windows (ReadDirectoryChangesW). No separate polling fallback needed.
