---
id: "2.4"
title: "Replace hardcoded tool names + standardize arrow keys"
priority: "P1"
estimated_time: "1.5h"
dependencies: ["1.gate"]
scope: "all"
breaking: false
---

# 2.4: Replace hardcoded tool names + standardize arrow keys

## Description

Two convention enforcement changes across model files:

1. Replace hardcoded `"Read"`, `"Write"`, `"Edit"`, `"Bash"`, `"Agent"` string comparisons with accessor functions from `parser/tools.go`.

2. Remove redundant `j`/`k` key handlers from Dashboard and SubAgent overlay, standardizing on `↑`/`↓` arrow keys only.

## Reference Files

- `design/tech-design.md` Integration 5 (Tool Accessors → Model files)
- `docs/conventions/tui-data-contracts.md` §1 (accessor functions)
- `docs/conventions/tui-ux-interaction.md` — arrow key navigation standard
- `internal/model/app.go` — hardcoded tool names at lines 595-617, 660-680, 1140-1151
- `internal/model/calltree.go:217` — existing `isAgentTool` (to be replaced with parser.IsAgentTool)
- `internal/model/dashboard.go` — j/k handlers to remove
- `internal/model/subagent_overlay.go` — j/k handlers to remove

## Acceptance Criteria

- [ ] `grep -rn '== "Read"\|"Write"\|"Edit"\|"Bash"\|"Agent"\|"SubAgent"' internal/model/app.go` returns no matches in comparison context
- [ ] `grep -rn 'case "j"\|case "k"' internal/model/dashboard.go internal/model/subagent_overlay.go` returns no matches
- [ ] `isAgentTool()` in calltree.go replaced with `parser.IsAgentTool()`
- [ ] Arrow key `↑`/`↓` works identically in Dashboard and SubAgent overlay
- [ ] `go test ./internal/model/ -v` passes

## User Stories

- **Story 2: Navigate All Panels with Consistent Arrow Keys**
  - Given any scrollable panel → `↑`/`↓` scrolls content identically
  - Given Dashboard or Overlay → no `j`/`k` handling exists

## Hard Rules

- All tool name comparisons use `parser.IsReadTool()`, `parser.IsEditTool()`, etc.
- `j`/`k` handlers completely removed — not commented out
- Detail panel already uses arrow keys only — no change needed

## Implementation Notes

### Tool accessor replacements in app.go:
1. Line 595: `case "Read":` → `if parser.IsReadTool(child.ToolName) {`
2. Line 606: `case "Write", "Edit":` → `if parser.IsEditTool(child.ToolName) {`
3. Lines 660-680: same pattern for `computeSubAgentStatsFromTurns`
4. Lines 1140-1151: `extractToolCommand` → already using stats function, update string comparisons

### Arrow key standardization:
1. `dashboard.go`: find `case "j"` / `case "k"` in Update(), remove handlers
2. `subagent_overlay.go`: find `case "j"` / `case "k"` in Update(), remove handlers
3. Verify `"up"` / `"down"` handlers exist and work in both files
