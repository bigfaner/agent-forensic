---
id: "3.2"
title: "Spec reconciliation (min-width + truncation format)"
priority: "P2"
estimated_time: "30min"
dependencies: ["2.gate"]
scope: "all"
breaking: false
---

# 3.2: Spec reconciliation (min-width + truncation format)

## Description

Align design documents to ensure consistency:
- P2-12: Verify terminal min-width is 80 columns across all design docs
- P2-14: Verify path truncation references same shared utility across all PRD UI functions

## Reference Files

- `prd/prd-spec.md` — Other Notes §Compatibility Requirements (min-width)
- `prd/prd-ui-functions.md` — UF-1 (path truncation format)
- `design/tech-design.md` — Dependencies section (min-width)
- `docs/conventions/tui-ux-interaction.md` — navigation conventions

## Acceptance Criteria

- [ ] All three documents (PRD spec, UI functions, tech design) state 80-column minimum
- [ ] All path truncation references in prd-ui-functions.md point to shared `truncatePathBySegment` utility
- [ ] No conflicting truncation format descriptions across documents
- [ ] `grep -rn 'min.*width\|minimum.*terminal\|80.*column\|80x24' prd/ design/` shows consistent values

## User Stories

No direct user story mapping — documentation alignment task (P2-12, P2-14).

## Hard Rules

- Only update documentation — no code changes
- Do NOT change acceptance criteria or behavior specifications — only align wording

## Implementation Notes

Check and fix:
1. `prd/prd-spec.md`: "Minimum terminal size: 80 columns x 24 rows" — verify present
2. `prd/prd-ui-functions.md`: UF-1 validation rules reference `truncatePathBySegment` — update if still references per-panel logic
3. `design/tech-design.md`: verify min-width stated in Dependencies or Error Handling section
