---
status: "completed"
started: "2026-05-14 02:12"
completed: "2026-05-14 02:17"
time_spent: "~5m"
---

# Task Record: 2.1 Fix CJK rendering in SubAgent overlay

## Summary
Replace byte-based width calculations with runewidth.StringWidth() in SubAgent overlay file ops section. Remove local truncatePath/truncRunes, use shared truncatePathBySegment from truncate.go. Fix path padding to use runewidth. Also fix dashboard_fileops.go which depended on the removed local truncatePath.

## Changes

### Files Created
无

### Files Modified
- internal/model/subagent_overlay.go
- internal/model/subagent_overlay_test.go
- internal/model/dashboard_fileops.go

### Key Decisions
- Also fixed dashboard_fileops.go which called the removed local truncatePath function, migrating it to shared truncatePathBySegment + runewidth.StringWidth for consistency
- Golden tests check CJK path rendering and UTF-8 validity rather than strict line-width bounds, since lipgloss border rendering adds width that varies

## Test Results
- **Tests Executed**: Yes
- **Passed**: 19
- **Failed**: 0
- **Coverage**: 85.5%

## Acceptance Criteria
- [x] Local truncatePath() function replaced with shared truncatePathBySegment()
- [x] Local truncRunes() function removed - uses shared version from truncate.go
- [x] Path padding in file ops section uses runewidth.StringWidth() instead of len()
- [x] grep len(displayPath)|len(path) returns no matches for width calculations
- [x] Golden test at 80x24 with CJK file paths passes
- [x] Golden test at 140x40 with CJK file paths passes
- [x] go test ./internal/model/ -run TestSubAgent -v passes

## Notes
dashboard_fileops.go also needed migration since it called the removed local truncatePath. Both files now consistently use runewidth.StringWidth for path padding.
