---
status: "completed"
started: "2026-05-14 02:26"
completed: "2026-05-14 02:29"
time_spent: "~3m"
---

# Task Record: 2.3 Fix hook panel overflow + CJK wrapping

## Summary
Fix hook panel overflow and CJK wrapping: renderHookStatsSection now uses width param for label truncation, renderHookMarkerSelected truncates at width boundary, dead local truncateStr removed, all hook labels respect panel borders using runewidth-aware truncation.

## Changes

### Files Created
无

### Files Modified
- internal/model/dashboard_hook_panel.go
- internal/model/dashboard_hook_panel_test.go

### Key Decisions
- renderHookStatsSection truncates label (FullID) portion only, leaving suffix (×N) intact; budget = width - suffixWidth
- renderHookMarkerSelected truncates baseLabel before applying color styles so ANSI codes don't inflate truncation budget
- Local truncateStr removed as dead code (zero callers); shared truncRunes from truncate.go used instead

## Test Results
- **Tests Executed**: Yes
- **Passed**: 22
- **Failed**: 0
- **Coverage**: 85.6%

## Acceptance Criteria
- [x] renderHookStatsSection signature updated: _ int → width int, used for label truncation
- [x] Hook labels truncate with truncRunes() at panel boundary, never extending past border
- [x] renderHookMarkerSelected also uses width parameter for truncation
- [x] Local truncateStr and wrapText functions removed — uses shared versions from truncate.go
- [x] grep -n 'func truncateStr|func wrapText' returns no matches
- [x] Golden test with hook label >30 chars: truncation visible, no overflow
- [x] Golden test with CJK hook description: wraps at display-width boundary, not rune count
- [x] Golden test with zero hook entries: empty state renders without crash
- [x] Golden test with one hook entry: single entry renders correctly
- [x] go test ./internal/model/ -run TestHook -v passes

## Notes
CJK wrapText was already working correctly via shared wrapText from truncate.go (task 1.1). The formatHookOutput function already called the shared version. The CJK test confirmed display-width-aware wrapping is correct.
