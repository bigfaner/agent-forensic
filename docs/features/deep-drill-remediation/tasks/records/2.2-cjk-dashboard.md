---
status: "completed"
started: "2026-05-14 02:17"
completed: "2026-05-14 02:25"
time_spent: "~8m"
---

# Task Record: 2.2 Fix CJK rendering in Dashboard (FileOps + Tool Stats)

## Summary
Replace byte-based len() width calculations with runewidth.StringWidth() in dashboard_fileops.go and dashboard.go for CJK rendering correctness

## Changes

### Files Created
无

### Files Modified
- internal/model/dashboard_fileops.go
- internal/model/dashboard.go
- internal/model/dashboard_fileops_test.go

### Key Decisions
- Used runewidth.StringWidth() for peak step name truncation and tool label width computation in dashboard.go
- Used truncRunes() from truncate.go for display-width-aware string slicing in dashboard.go instead of byte slicing
- Converted total count width calculations in dashboard_fileops.go from len() to utf8.RuneCountInString() for consistency with other count columns (pure ASCII digits)
- Tests use lipgloss.Width() for display-width verification instead of rune positions, correctly handling CJK double-width characters

## Test Results
- **Tests Executed**: Yes
- **Passed**: 569
- **Failed**: 0
- **Coverage**: 85.5%

## Acceptance Criteria
- [x] dashboard_fileops.go: all len() calls for path width replaced with runewidth.StringWidth()
- [x] dashboard_fileops.go: utf8.RuneCountInString() for count columns kept (pure ASCII digits per convention §7)
- [x] dashboard_fileops.go: path truncation uses truncatePathBySegment() from truncate.go
- [x] dashboard.go: tool name truncation uses runewidth.StringWidth() and shared truncRunes()
- [x] dashboard.go: peak step name truncation uses runewidth.StringWidth()
- [x] Golden test at 80x24 with CJK file paths + tool names passes
- [x] grep len() in dashboard_fileops.go returns no width-related matches
- [x] grep len(name)/len(e.name) in dashboard.go returns no matches
- [x] go test ./internal/model/ -run TestDashboard -v passes

## Notes
CJK tests added: TestFileOpsPanel_Render_CJKPathAlignment, TestFileOpsPanel_Render_CJKPathTruncation, TestFileOpsPanel_Render_NoLenForWidth, TestFileOpsPanel_Render_CJKGolden80x24, TestDashboard_CJKToolNameTruncation, TestDashboard_CJKPeakStepTruncation, TestDashboard_CJKToolNameAlignment. The peak step header line can exceed contentWidth for very long names - this is a pre-existing issue outside this task scope.
