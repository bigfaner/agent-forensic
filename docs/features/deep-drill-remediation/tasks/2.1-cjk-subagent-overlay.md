---
id: "2.1"
title: "Fix CJK rendering in SubAgent overlay"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
scope: "all"
breaking: false
---

# 2.1: Fix CJK rendering in SubAgent overlay

## Description

Replace byte-based width calculations with `runewidth.StringWidth()` in `internal/model/subagent_overlay.go`. Fix path truncation in the file ops section and padding calculations.

## Reference Files

- `design/tech-design.md` Integration 1 (Shared Truncation → SubAgent Overlay)
- `internal/model/subagent_overlay.go` — `truncatePath` (line 755), path padding (line 642)
- `internal/model/truncate.go` — shared `truncatePathBySegment`, `truncRunes` (from task 1.1)

## Acceptance Criteria

- [ ] Local `truncatePath()` function replaced with shared `truncatePathBySegment()` from truncate.go
- [ ] Local `truncRunes()` function removed — uses shared version from truncate.go
- [ ] Path padding in file ops section uses `runewidth.StringWidth()` instead of `len()`
- [ ] `grep -n 'len(displayPath)\|len(path)' internal/model/subagent_overlay.go` returns no matches for width calculations
- [ ] Golden test at 80x24 with CJK file paths passes — no corrupted UTF-8, correct column alignment
- [ ] Golden test at 140x40 with CJK file paths passes
- [ ] `go test ./internal/model/ -run TestSubAgent -v` passes

## User Stories

- **Story 1: View CJK File Paths Without Corruption** — overlay file ops section renders CJK paths with correct alignment
  - Given CJK file paths → all render without corruption in SubAgent overlay
  - Given mixed-width paths → column alignment correct (CJK=2 cols, ASCII=1 col)
- **Story 5: Understand File Path Context from Truncated Paths** — overlay uses segment-based truncation
  - Given long paths → truncated as `.../parent/file.go` format

## Hard Rules

- Remove local `truncatePath` and `truncRunes` definitions — use shared versions from truncate.go
- All width calculations in this file must use `runewidth.StringWidth()`
- Follow `docs/conventions/tui-dynamic-content.md` §3, §6

## Implementation Notes

Changes in `subagent_overlay.go`:
1. Remove `truncatePath` function (line 755-760)
2. Remove `truncRunes` function (line 356+) — already moved to truncate.go
3. Line 641: `truncatePath(e.path, pathWidth)` → `truncatePathBySegment(e.path, pathWidth)`
4. Line 642: `len(displayPath)` → `runewidth.StringWidth(displayPath)` for padding
5. Verify tool stats section already uses runewidth (lines 462, 469) — no change needed
