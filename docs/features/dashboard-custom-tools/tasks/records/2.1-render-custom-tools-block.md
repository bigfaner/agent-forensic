---
status: "completed"
started: "2026-05-11 21:21"
completed: "2026-05-12 09:30"
time_spent: "~12h 9m"
---

# Task Record: 2.1 实现 renderCustomToolsBlock 渲染函数

## Summary
Implemented renderCustomToolsBlock, renderSkillCol, renderMCPCol, renderHookCol in dashboard_custom_tools.go. Wide layout (width>=80, colWidth>=18) renders three columns side by side; narrow/very-narrow falls back to single-column stacked. StateLoading shows 计算中…, StateError shows 统计失败. Each column sorts by count desc then name asc, truncates at 10/5/10 items with '... +N more', truncates names at 22 runes. Wired into renderDashboard() before return. 29 new tests added.

## Changes

### Files Created
- internal/model/dashboard_custom_tools.go
- internal/model/dashboard_custom_tools_test.go

### Files Modified
- internal/model/dashboard.go

### Key Decisions
- Created separate file dashboard_custom_tools.go to keep dashboard.go clean
- Wide layout threshold: width>=80 AND colWidth=(width-6)/3>=18; otherwise single-column stacked
- StateLoading/StateError handled inside renderCustomToolsBlock (not just in renderContent) per UI design spec
- MCP footnote only rendered when MCPServers is non-empty

## Test Results
- **Tests Executed**: Yes
- **Passed**: 351
- **Failed**: 0
- **Coverage**: 80.1%

## Acceptance Criteria
- [x] 三类均无数据时返回 ""（区块不渲染）
- [x] width >= 80 时三列并排，每列宽度 (width - 6) / 3，最小 18；若 (width-6)/3 < 18 回退单列
- [x] width < 80 时单列堆叠，顺序 Skill → MCP → Hook，列间空一行
- [x] 无数据的列显示 (none)（fg-muted color "240"）
- [x] Skill 列：按调用次数降序，次数相同按名称字母升序；超过 10 条显示 ... +N more
- [x] MCP 列：server 按总次数降序；每 server 下工具按次数降序，次数相同按名称升序；超过 5 个工具显示 ... +N more；列标题附 *，区块底部注明 * 仅统计 mcp__ 前缀工具
- [x] Hook 列：按次数降序，次数相同按名称升序；超过 10 条显示 ... +N more
- [x] 名称超过 22 字符时截断并附 …（[]rune 截断）
- [x] go test ./internal/model/... 覆盖率 ≥ 80%
- [x] 计算中状态：数据区域显示 计算中…（fg-muted）
- [x] 统计失败状态：数据区域显示 统计失败（fg-muted）

## Notes
无
