# agent-forensic 使用指南

> 用 lazygit 的方式浏览你的 Claude Code 会话历史 — 一次按键即可看清 AI 做了什么、花了多久、有没有越界。

agent-forensic 是一个终端交互工具（TUI），用于回放和审查 Claude Code 的每次会话。你不再需要手动翻阅几千行的 JSONL 日志——所有信息都被组织成可视化的调用树，异常操作自动高亮标注。

**它能帮你：**
- 回顾某次会话里 AI 到底做了哪些操作
- 快速定位耗时异常或越权访问
- 实时监控正在运行的会话
- 统计各工具的使用频率和时间分布
- 深入分析 SubAgent（子智能体）的调用细节

---

## Quick Start

### 安装

从 [Releases](../../releases) 页面下载对应平台的二进制文件，或从源码构建：

```bash
git clone <repo-url> && cd agent-forensic
just build        # 需要 Go 1.26+
```

构建后当前目录生成 `agent-forensic` 可执行文件。

### 启动

```bash
./agent-forensic          # 中文界面（默认）
./agent-forensic -l en    # 英文界面
```

工具会自动扫描 `~/.claude/projects/` 下的所有会话记录（由 Claude Code 自动生成）。

### 第一次使用

启动后你会看到三栏界面：

```
┌─ Sessions ──────┐  ┌─ Call Tree ──────────────────────────────┐
│ ▸ 2026-05-12 .. │  │ Turn 1  用户: "Fix the bug"              │
│   2026-05-11 .. │  │   ├─ Read     src/main.go          0.2s │
│   2026-05-11 .. │  │   ├─ Bash     go test ./...        1.3s │
│   ...           │  │   └─ Write    src/main.go          0.1s │
│                 │  │ Turn 2  用户: "run again"                 │
│                 │  │   └─ Bash     go test ./...        0.8s │
├─────────────────┤  ├─────────────────────────────────────────┤
│                 │  │ Detail                                    │
│                 │  │ 选中任意节点，这里显示详细内容              │
└─────────────────┘  └─────────────────────────────────────────┘
```

**操作流程：**

1. 左侧选择一个会话（`↑`/`↓` 移动，`Enter` 选中）
2. 右上查看该会话的完整调用树
3. 选中某个工具调用，右下查看参数和输出
4. 按 `d` 一键查看所有异常，按 `s` 查看统计数据
5. 选中 SubAgent 节点时按 `a` 深入分析子智能体

**记住这 6 个键就够了：**

| 按键 | 作用 |
|------|------|
| `↑`/`↓` | 上下移动 |
| `Enter` | 确认选择 / 展开 |
| `Tab` | 切换面板焦点 |
| `d` | 查看异常诊断 |
| `s` | 查看统计数据 |
| `q` | 退出 |

---

## 详细指南

### 界面总览

工具由三个面板组成，通过 `Tab` 键切换焦点：

| 面板 | 位置 | 用途 |
|------|------|------|
| **Sessions** | 左侧 | 所有会话列表，支持按日期/文件名搜索 |
| **Call Tree** | 右上 | 当前会话的操作记录，按对话轮次分组 |
| **Detail** | 右下 | 选中操作的具体参数、输出和 AI 思考过程 |

底部状态栏显示当前可用的快捷键提示、界面语言和版本号。

---

### 会话列表

左侧面板列出你的 Claude Code 会话记录。启动时显示最近 20 条，按 `G` 或 `g` 可以加载更多。

**搜索会话：** 按 `/` 进入搜索，支持以下方式：
- 按日期查找：输入 `2026-05-12` 或 `05-12`
- 按文件名查找：输入文件名中包含的文字
- `Enter` 确认搜索，`Esc` 取消并恢复完整列表

---

### 调用树

选中一个会话后，右上方展示它的完整操作历史。一次对话被组织成多个 **Turn（轮次）**，每个 Turn 包含用户的指令和 AI 执行的所有工具调用：

```
Turn 1  用户: "Fix the bug in auth"
  ├─ Read       src/auth.go           0.3s
  ├─ Bash       go test ./auth        2.1s
  └─ Write      src/auth.go           0.1s
Turn 2  用户: "commit this"
  └─ Bash       git commit -m "..."   0.5s
```

**操作方式：**

| 按键 | 说明 |
|------|------|
| `↑`/`↓` | 上下移动光标 |
| `Enter` | 展开或折叠一个 Turn |
| `n` | 直接跳到下一个 Turn |
| `p` | 直接跳到上一个 Turn |
| `a` | 打开 SubAgent 深入分析面板（仅在 SubAgent/Agent 节点上可用） |

**选中 Turn 节点时**，Detail 面板会展示用户发送的完整指令和本轮的工具使用统计。

**SubAgent 摘要模式：** 当一个 SubAgent 包含超过 50 个子调用时，调用树不会逐个列出，而是显示一行摘要：

```
  └─ 128 sub-sessions (avg 2.3s, 4.7 tools/session)
```

**异常标记：**
- <span style="color:yellow">黄色</span> — 慢调用（超过 30 秒）
- <span style="color:red">红色</span> — 越权访问（读写项目目录外的文件）

---

### SubAgent 深入分析

在调用树中选中一个 SubAgent（或 Agent）工具节点后，按 `a` 打开全屏分析面板。该面板展示该 SubAgent 内部的完整调用统计：

**三个分区（`Tab` 键切换，空分区自动跳过）：**

| 分区 | 内容 |
|------|------|
| **Tool Stats** | 工具调用次数柱状图 + 时间占比，双栏布局 |
| **Hook Analysis** | Hook 统计（按类型分组计数）+ 按轮次的时间线（颜色标记） |
| **File Ops** | 文件级读写统计表（R/E/Total） |

面板标题显示 SubAgent 的 Command（如果有），方便识别。

用 `↑`/`↓` 在各分区内滚动，按 `Esc`、`q` 或 `a` 关闭面板。

---

### 详情面板

调用树中选中任何一个工具操作，右下方会显示它的完整信息：

- **工具参数** — 比如读取了哪个文件、执行了什么命令
- **执行结果** — 工具的输出内容，可滚动查看
- **AI 思考过程** — AI 在执行前的推理片段
- **敏感数据自动脱敏** — 包含 API_KEY、SECRET、TOKEN、PASSWORD 等关键词的值会被替换为 `***`

**SubAgent 统计视图：** 选中 SubAgent 的子调用节点时，Detail 面板会显示该子调用的工具统计摘要（工具分类、文件列表、平均/峰值耗时）。按 `Tab` 可在统计视图和普通工具详情之间切换。

| 按键 | 说明 |
|------|------|
| `↑`/`↓` | 上下滚动内容 |
| `Enter` | 在完整/截断视图间切换 |
| `Tab` | 切换 SubAgent 统计视图 / 工具详情视图（仅 SubAgent 子节点可用） |

---

### 诊断报告

按 `d` 打开当前会话的诊断报告，它会汇总所有检测到的异常：

```
┌─ 诊断报告 ──────────────────────────────────┐
│                                              │
│ ⚠ 慢调用 #1                                  │
│   Bash "go build ./..." 耗时 45.2s           │
│   位置: Turn 3, Step 2                       │
│   ── AI 思考 ──                              │
│   "This build is taking unusually long..."   │
│                                              │
│ 越权访问 #1                                   │
│   Write "/etc/config.yaml" (项目目录外)       │
│   位置: Turn 5, Step 1                       │
│                                              │
└──────────────────────────────────────────────┘
```

每条异常包含具体操作内容、发生位置和 AI 当时的思考。用方向键切换条目，按 `Enter` 可直接跳转到调用树的对应位置查看上下文。

按 `Esc` 或 `q` 关闭诊断报告。

---

### 统计面板

按 `s` 打开统计仪表盘，了解一次会话的全貌。仪表盘包含四个分区，通过 `Tab` 键切换：

| 分区 | 内容 |
|------|------|
| **Tool Stats** | 工具调用次数柱状图 + 时间占比，双栏布局 |
| **Custom Tools** | Skill、MCP 服务器工具的细分统计，按服务器分组 |
| **File Ops** | 文件级读写统计表（R/E/Total） |
| **Hook Analysis** | Hook 统计（按 FullID 分组计数）+ 按轮次的时间线，带颜色图例 |

顶部显示会话总耗时和峰值步骤。

**Hook 时间线说明：**
- 绿色标记 — `PreToolUse`
- 青色标记 — `PostToolUse`
- 黄色标记 — `Stop`
- 品红标记 — `user-prompt-submit-hook`

| 按键 | 说明 |
|------|------|
| `Tab` | 切换分区（Tool Stats → Custom Tools → File Ops → Hook Analysis） |
| `↑`/`↓` | 滚动内容（Hook Analysis 分区内导航） |
| `1` | 打开会话选择器，切换到其他会话的统计 |
| `r` | 刷新统计数据 |
| `s` 或 `Esc` | 关闭面板 |

---

### 实时监控

在调用树面板中按 `m` 可以开启实时监控模式。当你有一个正在运行的 Claude Code 会话时，新的操作会实时出现在调用树中，带有青色 `[NEW]` 标记闪烁提示（3 秒后自动消失）。

适用于以下场景：
- 让 Claude Code 在另一个终端窗口工作，同时在这里实时观察它的行为
- 监控长时间运行的自动化任务

再按一次 `m` 关闭监控。

---

### 语言切换

支持中文和英文两种界面语言：

- 启动时通过 `-l en` 或 `-l zh` 指定
- 运行中随时按 `L` 键即时切换

---

### 快捷键速查表

#### 全局操作

| 按键 | 说明 |
|------|------|
| `Tab` | 切换面板焦点（Sessions → Call Tree → Detail） |
| `1` | 直接跳到 Sessions 面板 |
| `2` | 直接跳到 Call Tree 面板 |
| `3` | 直接跳到 Detail 面板 |
| `L` | 切换中英文 |
| `q` | 退出 |

#### Sessions 面板

| 按键 | 说明 |
|------|------|
| `↑`/`↓` | 上下移动 |
| `Enter` | 选中会话 |
| `/` | 搜索（按日期或文件名） |
| `G` / `g` | 加载更多会话 |

#### Call Tree 面板

| 按键 | 说明 |
|------|------|
| `↑`/`↓` | 上下移动 |
| `Enter` | 展开/折叠 Turn |
| `n` / `p` | 跳到下一个/上一个 Turn |
| `m` | 开关实时监控 |
| `a` | 打开 SubAgent 深入分析面板（仅 SubAgent/Agent 节点） |
| `d` | 打开诊断报告 |
| `s` | 打开统计面板 |

#### Detail 面板

| 按键 | 说明 |
|------|------|
| `↑`/`↓` | 滚动内容 |
| `Enter` | 切换完整/截断视图 |
| `Tab` | 切换 SubAgent 统计视图 / 工具详情 |

#### 模态框内（诊断/统计/搜索）

| 按键 | 说明 |
|------|------|
| `Esc` | 关闭模态框 |
| `Enter` | 确认/跳转 |

#### SubAgent 分析面板

| 按键 | 说明 |
|------|------|
| `Tab` | 切换分区（Tool Stats → Hooks → File Ops） |
| `↑`/`↓` | 分区内滚动 |
| `Esc` / `q` / `a` | 关闭面板 |

---

### 常见问题

**启动后左侧面板为空？**

确保你已经用过 Claude Code，它会在 `~/.claude/projects/` 下自动生成会话记录。如果目录不存在或没有 `.jsonl` 文件，说明还没有可查看的会话。

**界面显示错乱？**

终端窗口需要至少 80 列 x 24 行。过小的终端会显示警告。

**某个会话显示为损坏？**

如果会话文件中超过一半的内容无法解析（可能是文件损坏或不完整），工具会标记为损坏会话并跳过。

**调用树中某个 SubAgent 显示 "N sub-sessions" 摘要而不是具体调用？**

当 SubAgent 包含超过 50 个子调用时，为避免界面过长，自动切换为摘要模式。如需查看详细统计，选中该节点后按 `a` 打开全屏分析面板。

---

### 典型使用场景

**场景 1：回顾昨天的工作**

按 `/` 输入昨天的日期 → 从结果中选择会话 → 浏览调用树了解 AI 做了什么。

**场景 2：排查一次耗时很长的会话**

选择会话 → 按 `s` 查看统计面板，找到耗时最长的操作 → 按 `d` 查看是否被标记为慢调用异常。

**场景 3：审查安全性**

选择会话 → 按 `d` 打开诊断报告 → 检查是否有越权文件访问的红色标记。

**场景 4：深入分析 SubAgent 行为**

在调用树中找到 SubAgent 节点 → 按 `a` 打开全屏分析 → 用 `Tab` 切换 Tool Stats / Hook Analysis / File Ops 分区，了解子智能体的具体行为。

**场景 5：实时跟踪正在运行的任务**

选择当前活跃会话 → 按 `m` 开启监控 → 在另一个终端观察 AI 的实时操作。

**场景 6：审查 Hook 执行情况**

按 `s` 打开统计面板 → 用 `Tab` 切换到 Hook Analysis 分区 → 查看各 Hook 的调用频率和按轮次的时间线分布。
